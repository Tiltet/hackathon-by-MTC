import { StyleSheet } from "react-native";

export const mainStyle = StyleSheet.create({
    container: {
        height: "100%",
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
    text: {
        fontSize: 50,
    }
});
